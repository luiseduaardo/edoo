/* inclui as bibliotecas que eu desejo usar no meu programa */
#include <iostream> // biblioteca de inputs e outputs (io)

/* indica onde está a biblioteca que vamos usar */
using namespace std;

/* função principal do meu código. estrutura:
    [retorno da função; tipo] [nome da função] ([entrada da função; tipo]))*/
int main() {

    cout << "hello, world\nCurso de C++" << endl;

    system("pause");
    return 0; // por padrão, o return 0 é dado quando o programa encerrou corretamente
}
