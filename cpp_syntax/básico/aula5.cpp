#include <iostream>
using namespace std;

// podemos declarar constantes globalmente (fora da função main)
#define pi 3.1415
#define nome cout << "luis eduardo"

int main() {

    // Não precisa usar mais de uma linha para declarar as variáveis, basta usar vírgula como separador
    int vidas = 3, tiros = 500, life = 100;

    /* ALTERNATIVA (não está errada, mas pode ser otimizada para deixar mais fácil a leitura do código)
    int vidas;
    int tiros;
    int life;
    OU
    int vidas; int tiros; int life;
    */

    nome;

    return 0;
}