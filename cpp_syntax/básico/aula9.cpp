#include <iostream>
using namespace std;

int main() {

    bool sol = true;

    // Usa-se os operadores de comparação:
        // >, <, >=, <=, ==, !=

    if (sol) {
        // Se o teste lógico for verdadeiro
        cout << "Vou à praia" << endl;
    } else {
        // Se o teste lógico for falso
        cout << "Vou ao cinema" << endl;
    }

    return 0;
}